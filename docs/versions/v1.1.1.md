# 版本 1.1.1 详细说明

发布日期：2025-01-XX

## 概述

1.1.1 是一个**紧急修复版本**，解决了 1.1.0 版本的依赖缺失问题，并确保作为通用组件的独立性和可预测性。

## 修复内容

### HttpClient5 依赖缺失问题

#### 问题描述
1.1.0 版本在使用方运行时报错：
```
java.lang.ClassNotFoundException: org.apache.hc.client5.http.config.ConnectionConfig
java.lang.ClassNotFoundException: org.apache.hc.core5.http.HttpEntity
java.lang.ClassNotFoundException: org.apache.hc.core5.io.ModalCloseable
```

#### 根本原因
1. 1.1.0 的 `pom.xml` 未显式声明 `httpcore5` 和 `httpcore5-h2` 依赖
2. 依赖传递不完整，使用方无法获取这些核心库
3. 作为通用组件，依赖了自身 Spring Boot BOM 管理版本，不够独立

#### 修复方案
**显式声明所有 HttpClient5 依赖，确保通用组件的独立性：**

```xml
<dependency>
    <groupId>org.apache.httpcomponents.client5</groupId>
    <artifactId>httpclient5</artifactId>
    <version>5.2.3</version>
</dependency>
<dependency>
    <groupId>org.apache.httpcomponents.core5</groupId>
    <artifactId>httpcore5</artifactId>
    <version>5.1.5</version>
</dependency>
<dependency>
    <groupId>org.apache.httpcomponents.core5</groupId>
    <artifactId>httpcore5-h2</artifactId>
    <version>5.1.5</version>
</dependency>
```

**版本选择理由：**
- `httpclient5 5.2.3`：稳定版本，广泛支持
- `httpcore5 5.1.5`：与 httpclient5 5.2.x 完全兼容
- 兼容 Spring Boot 2.6.x/2.7.x/3.x
- 不依赖使用方的 BOM，版本可预测

## 通用组件设计原则

1.1.1 版本遵循通用组件的最佳实践：

### 1. 独立性
- 所有依赖显式声明版本
- 不依赖使用方的依赖管理
- 不假设使用方的技术栈

### 2. 可预测性
- 依赖版本明确，不会被覆盖
- 行为一致，不受环境影响

### 3. 兼容性
- 选择稳定且广泛支持的版本
- 兼容主流 Spring Boot 版本
- 支持非 Spring Boot 项目

## 升级说明

### 从 1.1.0 升级到 1.1.1

**步骤 1**：修改依赖版本

```xml
<dependency>
    <groupId>com.jeesoul</groupId>
    <artifactId>jeesoul-ai-model</artifactId>
    <version>1.1.1</version>  <!-- 从 1.1.0 改为 1.1.1 -->
</dependency>
```

**步骤 2**：重新编译

```bash
mvn clean compile
```

**完成！** 运行时错误将被解决。

### 从 1.0.x 直接升级到 1.1.1

请参考：[升级到 1.1.0 指南](../migration/upgrade-to-1.1.0.md)

> 注意：指南中的版本号改为 `1.1.1` 即可。

## 影响范围

- **受影响版本**：1.1.0
- **修复版本**：1.1.1
- **影响场景**：所有使用 1.1.0 的项目在运行时都会报错
- **修复方式**：升级到 1.1.1

## 验证

升级后，启动应用并调用 AI 模型接口，确认：
- ✅ 应用启动成功
- ✅ AI 模型调用正常
- ✅ 无 `ClassNotFoundException` 错误

## 致歉

非常抱歉 1.1.0 版本给您带来的不便。我们已加强了发版前的测试流程，避免类似问题再次发生。

## 相关链接

- [CHANGELOG](../../CHANGELOG.md)
- [v1.1.0 详细说明](v1.1.0.md)
- [升级指南](../migration/upgrade-to-1.1.0.md)
